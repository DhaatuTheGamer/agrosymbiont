const { performance } = require('perf_hooks');

// A simple script to benchmark the WaitlistForm is a bit complex since it's React Testing Library.
// We can just run the test suite to see if it passes.
